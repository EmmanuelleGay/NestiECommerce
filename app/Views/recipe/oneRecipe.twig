{% extends "layout/base.twig" %}

{% block title %}
    Recette
{% endblock %}

{% block body %}
    <div class="container mt-3 border">
        <div class="row justify-content-around">
            <p class="col-md-8 text-center">{{ recipe.name }}</p>
            <p class="col-md-4 text-center">{{ recipe.difficulty }} /5</p>
        <div>
    </div>


{% endblock %}
